#初始化
git config --global user.name/user.email 'xxx'
#创建仓库
git init

#添加暂存
git add xxx
#提交暂存(描述)
git commit -m 'xxx'

#查看状态
git status
#查看不同,工作区和[|上次]暂存区(暂存区-仓库分支比较)
git diff xxx (--cached) (--staged)

#查看日志(简述)
git log/reflog (--pretty=oneline)
#版本回退多个/指定
git reset --hard HEAD^(~2)/xxx

